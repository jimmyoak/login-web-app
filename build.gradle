apply plugin: 'java'

version = '1.0'
def mainClass = 'main.java.Web.Server.Server'

jar {
    manifest {
        attributes 'Implementation-Title': 'LoginWebApp',
                'Implementation-Version': version,
                'Main-Class': mainClass
    }
}

dependencies {
    testCompile 'junit:junit:4.12'
}

repositories {
    mavenCentral()
}